import React from "react";

const Account = () => {
  return (

    <div style={{ minHeight: '60vh', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '1.2rem', color: '#888' }}>
      Profile is now shown as a floating modal. Please use the Profile button in the header.

    </div>
  );
};

export default Account;